# Draft PR Description — Runtime Fallback Orchestrator

Use issue [NUT-366](/NUT/issues/NUT-366) `pr-template` document as the canonical PR body.

This commit adds only the sanitized RFC copy for upstream reviewer context.
Implementation code changes follow in incremental commits on the same branch.
